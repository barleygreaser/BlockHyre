import { redirect } from "next/navigation";

// Sub-pages go here
export default function RenterDisputesRoot() {
    redirect("/dashboard"); // Redirect to main dashboard for now, until full page is built
}
